name: JARVIS Tests

on:
  push:
    branches: [main, jarvis-v1-foundation]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Run JARVIS tests
        run: python -m unittest discover -s jarvis/tests -v
