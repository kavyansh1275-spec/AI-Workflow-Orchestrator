# V1 CI Check

GitHub Actions workflow added at `.github/workflows/v1-tests.yml`.

It runs the V1 unit-test suite and a CLI smoke test on pushes and pull requests to `main`, and can also be started manually.
